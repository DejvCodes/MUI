import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: "#035EFC",
    },
    secondary: {
      main: "#15C630",
    },
    otherColor: {
      main: "#999",
    },
  },
})

export default theme